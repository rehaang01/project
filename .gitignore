# Virtual environment (1.7 GB, machine-specific) — recreate via requirements.txt
fpml/

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Competition data (not redistributed)
dataset/

# Redundant generated archive (rebuilt from src/)
flipkart_traffic_demand_source.tar.gz

# Experiment / sweep submission CSVs — keep only the final ones (whitelisted below)
submissions/*
!submissions/submission.csv
!submissions/submission_highscore.csv
!submissions/submission_robust.csv
!submissions/submission_memo.csv
